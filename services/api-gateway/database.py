"""Postgres access for the API gateway.

Phase 2 implements:
  - connection management (DATABASE_URL)
  - schema bootstrap: search_jobs, search_history tables
  - insert_pending_job(job_id, ...), get_job(job_id)
  - get_history(limit)
"""

# TODO(Phase 2): implement
