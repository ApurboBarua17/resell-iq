// API client for the FastAPI gateway.
// TODO(Phase 3): implement
//   search(itemDescription, condition, category)  → POST /api/search
//   pollResult(jobId)                             → GET /api/result/:jobId
//   getHistory(limit)                             → GET /api/history
//   getCacheStats()                               → GET /api/cache-stats
